language: java
install: true
jdk: oraclejdk8

git:
  depth: false  # get full git history for SQ blame info

script: "./travis.sh"
cache:
  directories:
  - "$HOME/.sonar"
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
notifications:
  email: false
  on_start: always
